
import * as fs from 'fs';
import {ReadStream} from "fs";
import {parse} from "csv-parse";
import ContactsHelper from "../../helpers/contacts/ContactsHelper";

/**
 * Class to handle csv reads and write from and to file respectively
 */
export default class HandleCsvFiles {

    /**
     * Read uploaded csv file
     * @param file
     * @param group
     */
    public static async readCsvFile(file: any, group: number) {
    }

}